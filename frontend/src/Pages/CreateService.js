import React from 'react'

const CreateService = () => {
    return (
        <div style=
        {{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            height: '90vh'
        }}
        >
            <h1>CreateService</h1>
        </div>

    )
}

export default CreateService;